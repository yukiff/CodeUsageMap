# RepresentativeSample

Windows と Mac の両方で再利用する代表検証用 solution。

含む観点:

- interface dispatch
- DI registration (`AddScoped`, `AddTransient(typeof, typeof)`)
- event subscription
- override
- test project reference
